# tmux

Semi-stolen, semi-handcrafted.

## What's Here

- `C-a` prefix (not `C-b`)
- Vim-style pane navigation (`hjkl`)
- Project/session switchers (`n`, `C-f`, `g`)
- OpenCode popup toggle (`C-o`)
- One Dark-ish status bar
